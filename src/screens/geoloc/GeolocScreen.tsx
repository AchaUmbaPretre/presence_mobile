import React from "react";
import { StyleSheet, Text, View } from "react-native";

const GeolocScreen = () => {
  return (
    <View>
      <Text>GeolocScreen</Text>
    </View>
  );
};

export default GeolocScreen;

const styles = StyleSheet.create({});
